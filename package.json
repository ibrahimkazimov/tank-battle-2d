{
  "name": "tank-battle-2d-v2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:server": "npm run start --workspace=@tank-battle/server",
    "dev:client": "npm run dev --workspace=@tank-battle/client",
    "build": "npm install && npm run build --workspace=@tank-battle/client"
  }
}
